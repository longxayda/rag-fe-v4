# Dify API Configuration
VITE_API_BASE_URL=
VITE_API_KEY=

# Base URL of admin API; must end with /api (e.g. http://localhost:5000/api)
VITE_BACKEND_URL=http://localhost:5000/api
VITE_STATIC_URL=

# Optional: Mapbox token for Mapbox satellite/vector style. If not set, map still uses Mapbox GL with OpenStreetMap tiles (no API).
# VITE_MAPBOX_PK=pk.your_mapbox_public_token
