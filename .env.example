# Dify API Configuration
VITE_API_BASE_URL=
VITE_API_KEY=

# Base URL of admin API; must end with /api (e.g. http://localhost:5000/api)
VITE_BACKEND_URL=http://localhost:5000/api
VITE_STATIC_URL=

# Optional: Mapbox token. If set, the map uses Mapbox (satellite-streets). If not set, the map uses Leaflet + OpenStreetMap (no token required).
# VITE_MAPBOX_PK=pk.your_mapbox_public_token
